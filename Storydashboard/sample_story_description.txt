Set background explicitly set to pure white (#FFFFFF), ensuring a clean, neutral canvas suitable for brand visuals. Load the Eight product icons are loaded in a fixed sequence: Google Sheets, Gmail, Google Drive, Google Docs, Google Slides, Google Calendar, Google Meet, and YouTube (used here to represent Google Vids). Each asset is instantiated as an `ImageMobject` via a helper function (make_icon) that normalizes icon height to 0.9 units, and all icons are grouped using a `Group` to ensure compatibility with image-based mobjects. The initial visual structure is a compact 3×3 grid defined using explicit Cartesian coordinates. Horizontal and vertical spacing are both set to 0.5 units (`d_x = 0.5`, `d_y = 0.5`). The eight outer dot positions are defined as exact vectors: top-left at (-0.5, 0.5, 0), top-center at (0, 0.5, 0), top-right at (0.5, 0.5, 0), mid-left at (-0.5, 0, 0), mid-right at (0.5, 0, 0), bottom-left at (-0.5, -0.5, 0), bottom-center at (0, -0.5, 0), and bottom-right at (0.5, -0.5, 0). A ninth dot is placed exactly at the origin (0, 0, 0) to serve as the center anchor. Each dot is rendered as a filled circle with a radius of 0.15, no stroke, full opacity, and a uniform grey color (#5F6368), matching Google’s neutral UI palette. All nine dots fade into view together with a slight upward positional offset, reinforcing a soft, non-mechanical entrance; Each icon is initially stacked directly on top of its corresponding dot position, inheriting that exact coordinate. At this stage, every icon is scaled down to a height of 0.2 units, visually binding it to the dot beneath and reinforcing the idea that icons originate from these nodes. The explosion phase transforms the grid into a large spatial spread. Destination positions are explicitly defined using expanded coordinates with `R_x = 2.4` and `R_y = 2.4`. Icons move to the following absolute positions: Sheets to (-2.4, 2.4, 0), Gmail to (0, 2.4, 0), Drive to (2.4, 2.4, 0), Docs to (-2.4, 0, 0), Slides to (2.4, 0, 0), Calendar to (-2.4, -2.4, 0), Meet to (0, -2.4, 0), and YouTube/Vids to (2.4, -2.4, 0). During this transformation, each icon fades in with a slight upward shift while simultaneously scaling from 0.2 to 1.0, preserving proportional clarity. In parallel, each corresponding dot animates toward the same destination position, scaling down to 0.1 and fading to zero opacity, visually handing off its role to the icon. The central dot collapses entirely by scaling to zero and fading out. All explosion animations are executed together inside an `AnimationGroup` using the `ease_out_back` rate function, producing a deliberately bouncy, elastic expansion that conveys energy and momentum rather than linear motion; After the explosion completes, a subtle stabilization pass occurs. Every icon performs a synchronized scale pulse to 1.08 and back to its original size using the `there_and_back` rate function. This micro-interaction acts as a visual settle, preventing the exploded layout from feeling abrupt or unfinished; The typography sequence begins immediately after the icon layout stabilizes. The word “Google” is created as a bold text object with a font size of 72, and each character is colored using a left-to-right gradient that precisely matches Google’s brand colors: blue (#4285F4), red (#DB4437), yellow (#F4B400), blue again, green (#0F9D58), and red. The word “Workspace” is created separately in bold with the same font size but rendered entirely in grey (#5F6368). Both text elements are arranged horizontally with zero buffer spacing, aligned as a single visual unit, positioned exactly at the origin, scaled down uniformly to 0.6, and set to zero opacity so they remain invisible during setup; The collapse phase converts spatial complexity into brand clarity. Each icon accelerates toward the origin while scaling down to zero, effectively collapsing the entire ecosystem into a single focal point. This motion uses the `ease_in_back` rate function, creating a slight reverse pull before the inward rush, which enhances the feeling of intentional convergence. Once all icons have fully collapsed and disappeared, the text reveal occurs instantly. The combined “Google Workspace” text scales from 0.6 to its natural scale of 1.0 while its opacity jumps to full visibility, using the `ease_out_back` rate function to introduce a gentle overshoot that mirrors the earlier explosion language and visually ties the sequence together.; The scene holds on the final text momentarily, allowing the branding to register clearly and conclude the section without introducing any additional motion or visual noise.
